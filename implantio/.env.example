# Public site URL — used for canonical URLs, sitemap and Open Graph tags
NEXT_PUBLIC_SITE_URL=https://implantio.com

# Optional: where lead notifications are forwarded from /api/lead
# (see src/app/api/lead/route.ts for wiring instructions)
LEAD_WEBHOOK_URL=
LEAD_NOTIFICATION_EMAIL=
